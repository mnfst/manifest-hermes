name: manifest
version: 0.5.1
manifest_version: 2
api_version: 1
license: MIT
description: >-
  Start the Manifest Python SDK inside Hermes: every HTTP call the agent makes
  is tracked, and the failures Manifest has a patch for are repaired.
author: Manifest
tags:
  - reliability
  - http
requires_env:
  - name: MNFST_KEY
    description: Manifest project key (Project Settings, Autofix on)
    url: https://manifest.build
    secret: true
